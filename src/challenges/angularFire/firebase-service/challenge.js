var MyCtrl = function($firebase, myFirebaseUtils) {
    //Creates a reference to the firebase database.
    this.dbRef = new Firebase(myFirebaseUtils.getBaseUrl());

    /*
    TODO: Create a refrence to the Firebase data with the
    $firebase service and assign it to this.fbData
    */


    /*
     TODO: Use the $update method to set this.fbData.lorem to: "my sample text"
     */

};